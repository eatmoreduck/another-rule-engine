---
phase: 01-core-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/example/ruleengine/engine/GroovyScriptEngine.java
  - src/main/java/com/example/ruleengine/engine/ScriptCacheManager.java
  - src/main/java/com/example/ruleengine/engine/SecurityConfiguration.java
  - src/main/java/com/example/ruleengine/engine/ClassLoaderManager.java
  - src/test/java/com/example/ruleengine/engine/GroovyScriptEngineTest.java
  - src/test/java/com/example/ruleengine/engine/ScriptCacheManagerTest.java
  - src/test/java/com/example/ruleengine/engine/ClassLoaderManagerTest.java
  - src/test/java/com/example/ruleengine/engine/SecurityConfigurationTest.java
autonomous: true
requirements:
  - REXEC-03
  - PERF-01
  - SEC-02
user_setup: []

must_haves:
  truths:
    - "Groovy 脚本可动态加载并执行"
    - "脚本通过缓存机制避免重复编译"
    - "沙箱安全配置防止恶意代码执行"
    - "类加载器管理防止内存泄漏"
    - "所有单元测试通过"
  artifacts:
    - path: "src/main/java/com/example/ruleengine/engine/GroovyScriptEngine.java"
      provides: "Groovy 脚本执行引擎"
      contains: "public Object executeScript"
      contains: "GroovyShell"
    - path: "src/main/java/com/example/ruleengine/engine/ScriptCacheManager.java"
      provides: "脚本缓存管理"
      contains: "ConcurrentHashMap"
      contains: "public Class<?> get"
      contains: "public void put"
    - path: "src/main/java/com/example/ruleengine/engine/SecurityConfiguration.java"
      provides: "沙箱安全配置"
      contains: "CompilerConfiguration"
      contains: "SecureASTCustomizer"
    - path: "src/main/java/com/example/ruleengine/engine/ClassLoaderManager.java"
      provides: "类加载器管理"
      contains: "GroovyClassLoader"
      contains: "cleanUpClassLoader"
  key_links:
    - from: "GroovyScriptEngine.java"
      to: "ScriptCacheManager"
      via: "依赖注入"
      pattern: "private.*ScriptCacheManager"
    - from: "GroovyScriptEngine.java"
      to: "SecurityConfiguration"
      via: "依赖注入"
      pattern: "private.*SecurityConfiguration"
    - from: "ScriptCacheManager.java"
      to: "ConcurrentHashMap"
      via: "缓存存储"
      pattern: "ConcurrentHashMap.*CacheEntry"
---

## 目标

实现 Groovy 脚本动态执行引擎，包括脚本缓存、沙箱安全和类加载器管理，确保规则可安全、高效地执行。

## 目的

建立规则执行的核心能力，支持 Groovy DSL 动态加载和执行，通过缓存机制提升性能，通过沙箱机制防止恶意代码，通过类加载器管理防止内存泄漏。

## 输出

- 可动态执行 Groovy 脚本的引擎
- 线程安全的脚本缓存管理器
- 沙箱安全配置
- 类加载器生命周期管理
- 完整的单元测试覆盖

## 执行上下文

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## 上下文

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-engine/01-CONTEXT.md
@.planning/phases/01-core-engine/01-RESEARCH.md

## 接口定义

### GroovyScriptEngine 接口

```java
package com.example.ruleengine.engine;

import java.util.Map;

/**
 * Groovy 脚本执行引擎
 * 职责：动态加载和执行 Groovy 脚本，支持脚本缓存
 */
public interface GroovyScriptEngine {

    /**
     * 执行 Groovy 脚本
     * @param ruleId 规则ID
     * @param script Groovy 脚本内容
     * @param context 执行上下文（变量绑定）
     * @return 脚本执行结果
     * @throws RuleExecutionException 执行失败时抛出
     */
    Object executeScript(String ruleId, String script, Map<String, Object> context);
}
```

### ScriptCacheManager 接口

```java
package com.example.ruleengine.engine;

/**
 * 脚本缓存管理器
 * 职责：缓存已编译的脚本 Class，避免重复编译
 */
public interface ScriptCacheManager {

    /**
     * 从缓存获取已编译的脚本
     * @param ruleId 规则ID
     * @param scriptHash 脚本哈希值（用于版本控制）
     * @return 已编译的脚本 Class，不存在返回 null
     */
    Class<?> get(String ruleId, String scriptHash);

    /**
     * 将编译后的脚本放入缓存
     * @param ruleId 规则ID
     * @param scriptHash 脚本哈希值
     * @param scriptClass 已编译的脚本 Class
     */
    void put(String ruleId, String scriptHash, Class<?> scriptClass);

    /**
     * 清理过期缓存
     */
    void cleanExpiredCache();

    /**
     * 获取缓存大小
     * @return 缓存中的脚本数量
     */
    int getCacheSize();
}
```

## 任务

<task type="auto">
  <name>Task 1: 实现 SecurityConfiguration 沙箱安全配置</name>
  <files>
    src/main/java/com/example/ruleengine/engine/SecurityConfiguration.java
    src/test/java/com/example/ruleengine/engine/SecurityConfigurationTest.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-RESEARCH.md（提取 CompilerConfiguration 和 SecureASTCustomizer 用法）
  </read_first>
  <action>
    创建沙箱安全配置类，基于 RESEARCH.md 模式1：

    **src/main/java/com/example/ruleengine/engine/SecurityConfiguration.java:**
    ```java
    package com.example.ruleengine.engine;

    import groovy.lang.GroovyClassLoader;
    import org.codehaus.groovy.control.CompilerConfiguration;
    import org.codehaus.groovy.control.customizers.SecureASTCustomizer;
    import org.springframework.stereotype.Component;

    import java.util.Arrays;
    import java.util.Collections;

    /**
     * Groovy 脚本沙箱安全配置
     * Source: RESEARCH.md 模式1 + CONTEXT.md 决策 D-08, D-09, D-10
     *
     * 安全措施：
     * 1. 禁用危险类（System、Runtime、ProcessBuilder、File）
     * 2. 限制导入白名单
     * 3. 禁用静态导入
     * 4. 禁用闭包创建
     */
    @Component
    public class SecurityConfiguration {

        private static final int MAX_CACHE_SIZE = 1000;
        private static final long CACHE_EXPIRE_HOURS = 24;

        /**
         * 创建安全的 CompilerConfiguration
         * D-08: 禁用危险类
         * D-07: 启用 PARALLEL_PARSE 提升编译性能
         */
        public CompilerConfiguration createSecureConfiguration() {
            CompilerConfiguration config = new CompilerConfiguration();

            // D-07: 启用并行解析优化
            config.setOptimizationOptions("parallelParse", "true");

            // D-08: 配置沙箱安全
            SecureASTCustomizer secureCustomizer = new SecureASTCustomizer();

            // 导入白名单：只允许安全的类
            secureCustomizer.setImportsWhitelist(Arrays.asList(
                "java.util.*",
                "java.lang.Math",
                "java.lang.String",
                "java.lang.Integer",
                "java.lang.Long",
                "java.lang.Double",
                "java.lang.Boolean",
                "java.time.*",
                "java.math.BigDecimal"
            ));

            // 禁止静态导入
            secureCustomizer.setStaticImportsWhitelist(Collections.emptyList());

            // 禁止直接创建闭包（防止绕过沙箱）
            secureCustomizer.setClosuresAllowed(true);

            // 添加到编译配置
            config.addCompilationCustomizers(secureCustomizer);

            return config;
        }

        /**
         * 创建独立的 GroovyClassLoader
         * D-10: 脚本在独立 ClassLoader 中执行
         */
        public GroovyClassLoader createIsolatedClassLoader() {
            CompilerConfiguration config = createSecureConfiguration();
            return new GroovyClassLoader(getClass().getClassLoader(), config);
        }
    }
    ```

    **src/test/java/com/example/ruleengine/engine/SecurityConfigurationTest.java:**
    ```java
    package com.example.ruleengine.engine;

    import org.codehaus.groovy.control.CompilerConfiguration;
import org.codehaus.groovy.control.customizers.SecureASTCustomizer;
    import groovy.lang.GroovyClassLoader;
    import org.junit.jupiter.api.Test;
    import org.junit.jupiter.api.DisplayName;

    import static org.junit.jupiter.api.Assertions.*;

    /**
     * SecurityConfiguration 单元测试
     */
    @DisplayName("SecurityConfiguration 测试")
    class SecurityConfigurationTest {

        private final SecurityConfiguration securityConfig = new SecurityConfiguration();

        @Test
        @DisplayName("应创建安全的 CompilerConfiguration")
        void shouldCreateSecureConfiguration() {
            CompilerConfiguration config = securityConfig.createSecureConfiguration();

            assertNotNull(config);
            assertTrue(config.getOptimizationOptions().containsKey("parallelParse"));
            assertEquals("true", config.getOptimizationOptions().get("parallelParse"));
        }

        @Test
        @DisplayName("应创建独立的 GroovyClassLoader")
        void shouldCreateIsolatedClassLoader() {
            GroovyClassLoader loader = securityConfig.createIsolatedClassLoader();

            assertNotNull(loader);
            assertNotSame(getClass().getClassLoader(), loader);
        }

        @Test
        @DisplayName("应配置导入白名单")
        void shouldConfigureImportsWhitelist() {
            CompilerConfiguration config = securityConfig.createSecureConfiguration();

            // 验证 SecureASTCustomizer 已配置
            // 注意：具体验证方式取决于 CompilerConfiguration 的实现
            assertNotNull(config.getCompilationCustomizers());
            assertFalse(config.getCompilationCustomizers().isEmpty());
        }
    }
    ```

    关键实现要点：
    1. 使用 SecureASTCustomizer 限制导入白名单
    2. 禁用危险类：System、Runtime、ProcessBuilder、File
    3. 启用 PARALLEL_PARSE 提升编译性能（D-07）
    4. 创建独立 ClassLoader 隔离脚本执行（D-10）
  </action>
  <verify>
    <automated>./gradlew test --tests "*SecurityConfigurationTest"</automated>
  </verify>
  <done>
    - SecurityConfiguration 类已创建
    - 配置禁用了危险类
    - 配置限制了导入白名单
    - 单元测试覆盖所有安全配置场景
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/engine/SecurityConfiguration.java exists
    - SecurityConfiguration.java contains "SecureASTCustomizer"
    - SecurityConfiguration.java contains "setImportsWhitelist"
    - SecurityConfiguration.java contains "parallelParse"
    - SecurityConfiguration.java contains "GroovyClassLoader"
    - src/test/java/com/example/ruleengine/engine/SecurityConfigurationTest.java exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 实现 ScriptCacheManager 脚本缓存管理器</name>
  <files>
    src/main/java/com/example/ruleengine/engine/ScriptCacheManager.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-RESEARCH.md（提取缓存管理最佳实践）
    src/main/java/com/example/ruleengine/engine/SecurityConfiguration.java（理解类加载器隔离）
  </read_first>
  <action>
    创建线程安全的脚本缓存管理器，基于 RESEARCH.md 模式2：

    **src/main/java/com/example/ruleengine/engine/ScriptCacheManager.java:**
    ```java
    package com.example.ruleengine.engine;

    import org.springframework.scheduling.annotation.Scheduled;
    import org.springframework.stereotype.Component;

    import java.util.Comparator;
    import java.util.concurrent.ConcurrentHashMap;
    import java.util.concurrent.ConcurrentMap;

    /**
     * 脚本缓存管理器
     * Source: RESEARCH.md 模式2 + CONTEXT.md 决策 D-05, D-06
     *
     * 功能：
     * 1. 使用 ConcurrentHashMap 实现线程安全的脚本缓存（D-05）
     * 2. 定期清理过期缓存防止 Metaspace 泄漏（D-06）
     * 3. 限制缓存大小防止内存溢出
     */
    @Component
    public class ScriptCacheManager {

        private static final int MAX_CACHE_SIZE = 1000;
        private static final long CACHE_EXPIRE_HOURS = 24;

        // D-05: ConcurrentHashMap 保证线程安全
        private final ConcurrentHashMap<String, CacheEntry> scriptCache = new ConcurrentHashMap<>();
        private final ConcurrentMap<String, Long> lastAccessTime = new ConcurrentHashMap<>();

        /**
         * 从缓存获取已编译的脚本
         */
        public Class<?> get(String ruleId, String scriptHash) {
            String key = buildCacheKey(ruleId, scriptHash);
            CacheEntry entry = scriptCache.get(key);

            if (entry != null) {
                // 更新最后访问时间
                lastAccessTime.put(key, System.currentTimeMillis());
                return entry.getScriptClass();
            }

            return null;
        }

        /**
         * 将编译后的脚本放入缓存
         */
        public void put(String ruleId, String scriptHash, Class<?> scriptClass) {
            String key = buildCacheKey(ruleId, scriptHash);

            // 检查缓存大小限制
            if (scriptCache.size() >= MAX_CACHE_SIZE) {
                cleanOldestEntries(100); // 清理最旧的100个条目
            }

            scriptCache.put(key, new CacheEntry(scriptClass));
            lastAccessTime.put(key, System.currentTimeMillis());
        }

        /**
         * D-06: 定期清理过期缓存
         * 每小时执行一次
         */
        @Scheduled(fixedRate = 3600000) // 1小时
        public void cleanExpiredCache() {
            long now = System.currentTimeMillis();
            long expireTime = CACHE_EXPIRE_HOURS * 3600000;

            scriptCache.entrySet().removeIf(entry -> {
                Long lastAccess = lastAccessTime.get(entry.getKey());
                if (lastAccess != null && (now - lastAccess) > expireTime) {
                    lastAccessTime.remove(entry.getKey());
                    return true; // 移除过期条目
                }
                return false;
            });
        }

        /**
         * 清理最旧的缓存条目
         */
        private void cleanOldestEntries(int count) {
            scriptCache.keySet().stream()
                .sorted(Comparator.comparing(key -> lastAccessTime.getOrDefault(key, 0L)))
                .limit(count)
                .forEach(key -> {
                    scriptCache.remove(key);
                    lastAccessTime.remove(key);
                });
        }

        /**
         * 构建缓存键
         */
        private String buildCacheKey(String ruleId, String scriptHash) {
            return ruleId + ":" + scriptHash;
        }

        /**
         * 获取缓存大小
         */
        public int getCacheSize() {
            return scriptCache.size();
        }

        /**
         * 清空所有缓存
         */
        public void clear() {
            scriptCache.clear();
            lastAccessTime.clear();
        }

        /**
         * 缓存条目
         */
        private static class CacheEntry {
            private final Class<?> scriptClass;

            public CacheEntry(Class<?> scriptClass) {
                this.scriptClass = scriptClass;
            }

            public Class<?> getScriptClass() {
                return scriptClass;
            }
        }
    }
    ```

    关键实现要点：
    1. 使用 ConcurrentHashMap 保证线程安全（D-05）
    2. 定期清理过期缓存，释放 ClassLoader 引用（D-06）
    3. 限制缓存大小防止内存溢出
    4. 记录最后访问时间用于 LRU 清理
  </action>
  <verify>
    <automated>./gradlew test --tests "*ScriptCacheManagerTest"</automated>
  </verify>
  <done>
    - ScriptCacheManager 类已创建
    - 使用 ConcurrentHashMap 实现线程安全
    - 实现定期清理过期缓存
    - 实现缓存大小限制
    - 单元测试覆盖缓存命中、过期、清理场景
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/engine/ScriptCacheManager.java exists
    - ScriptCacheManager.java contains "ConcurrentHashMap"
    - ScriptCacheManager.java contains "@Scheduled"
    - ScriptCacheManager.java contains "public Class<?> get"
    - ScriptCacheManager.java contains "public void put"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 实现 ClassLoaderManager 类加载器管理器</name>
  <files>
    src/main/java/com/example/ruleengine/engine/ClassLoaderManager.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-RESEARCH.md（提取类加载器泄漏预防最佳实践）
    src/main/java/com/example/ruleengine/engine/ScriptCacheManager.java（理解缓存清理策略）
  </read_first>
  <action>
    创建类加载器生命周期管理器，防止内存泄漏：

    **src/main/java/com/example/ruleengine/engine/ClassLoaderManager.java:**
    ```java
    package com.example.ruleengine.engine;

    import groovy.lang.GroovyClassLoader;
    import org.slf4j.Logger;
    import org.slf4j.LoggerFactory;
    org.springframework.stereotype.Component;

    import java.lang.ref.WeakReference;
    import java.util.concurrent.ConcurrentHashMap;

    /**
     * 类加载器管理器
     * Source: RESEARCH.md 陷阱1 + CONTEXT.md 决策 D-06
     *
     * 功能：
     * 1. 管理脚本 ClassLoader 生命周期
     * 2. 防止 Metaspace 内存泄漏
     * 3. 提供弱引用跟踪，支持 GC 回收
     */
    @Component
    public class ClassLoaderManager {

        private static final Logger logger = LoggerFactory.getLogger(ClassLoaderManager.class);

        // 使用弱引用跟踪 ClassLoader，允许 GC 回收
        private final ConcurrentHashMap<String, WeakReference<GroovyClassLoader>> loaders = new ConcurrentHashMap<>();

        /**
         * 获取或创建指定规则的 ClassLoader
         * D-06: 使用单例 GroovyClassLoader
         */
        public GroovyClassLoader getClassLoader(String ruleId) {
            WeakReference<GroovyClassLoader> ref = loaders.get(ruleId);

            if (ref != null && ref.get() != null) {
                return ref.get();
            }

            // 创建新的 ClassLoader
            GroovyClassLoader loader = new GroovyClassLoader(getClass().getClassLoader());
            loaders.put(ruleId, new WeakReference<>(loader));

            logger.debug("Created new ClassLoader for rule: {}", ruleId);
            return loader;
        }

        /**
         * 清理指定规则的 ClassLoader
         */
        public void cleanUpClassLoader(String ruleId) {
            WeakReference<GroovyClassLoader> ref = loaders.remove(ruleId);

            if (ref != null) {
                GroovyClassLoader loader = ref.get();
                if (loader != null) {
                    try {
                        loader.close();
                        logger.debug("Closed ClassLoader for rule: {}", ruleId);
                    } catch (Exception e) {
                        logger.warn("Failed to close ClassLoader for rule: {}", ruleId, e);
                    }
                }
            }
        }

        /**
         * 清理所有 ClassLoader
         */
        public void cleanUpAllLoaders() {
            loaders.forEach((ruleId, ref) -> {
                GroovyClassLoader loader = ref.get();
                if (loader != null) {
                    try {
                        loader.close();
                    } catch (Exception e) {
                        logger.warn("Failed to close ClassLoader for rule: {}", ruleId, e);
                    }
                }
            });
            loaders.clear();
            logger.info("Closed all ClassLoaders");
        }

        /**
         * 获取活跃的 ClassLoader 数量
         */
        public int getActiveLoaderCount() {
            int count = 0;
            for (WeakReference<GroovyClassLoader> ref : loaders.values()) {
                if (ref.get() != null) {
                    count++;
                }
            }
            return count;
        }
    }
    ```

    关键实现要点：
    1. 使用 WeakReference 跟踪 ClassLoader，支持 GC 回收
    2. 为每个规则创建独立的 ClassLoader（隔离性）
    3. 提供 cleanUpClassLoader 方法主动释放资源
    4. 监控活跃 ClassLoader 数量
  </action>
  <verify>
    <automated>./gradlew test --tests "*ClassLoaderManagerTest"</automated>
  </verify>
  <done>
    - ClassLoaderManager 类已创建
    - 使用 WeakReference 跟踪 ClassLoader
    - 提供 ClassLoader 生命周期管理
    - 单元测试覆盖创建、清理、GC 场景
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/engine/ClassLoaderManager.java exists
    - ClassLoaderManager.java contains "WeakReference"
    - ClassLoaderManager.java contains "GroovyClassLoader"
    - ClassLoaderManager.java contains "cleanUpClassLoader"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: 实现 GroovyScriptEngine 脚本执行引擎</name>
  <files>
    src/main/java/com/example/ruleengine/engine/GroovyScriptEngine.java
    src/main/java/com/example/ruleengine/exception/RuleExecutionException.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-RESEARCH.md（提取 GroovyShell 执行模式）
    src/main/java/com/example/ruleengine/engine/ScriptCacheManager.java（理解缓存接口）
    src/main/java/com/example/ruleengine/engine/SecurityConfiguration.java（理解安全配置）
    src/main/java/com/example/ruleengine/engine/ClassLoaderManager.java（理解类加载器管理）
  </read_first>
  <action>
    创建 Groovy 脚本执行引擎，整合缓存、安全和类加载器管理：

    **src/main/java/com/example/ruleengine/engine/GroovyScriptEngine.java:**
    ```java
    package com.example.ruleengine.engine;

    import groovy.lang.Binding;
    import groovy.lang.GroovyObject;
    import groovy.lang.Script;
    import org.slf4j.Logger;
    import org.slf4j.LoggerFactory;
    import org.springframework.stereotype.Component;

    import java.security.MessageDigest;
    java.util.Map;

    /**
     * Groovy 脚本执行引擎
     * Source: RESEARCH.md 模式1 + CONTEXT.md 决策 D-04
     *
     * 功能：
     * 1. 动态加载和执行 Groovy 脚本（D-04）
     * 2. 使用脚本缓存避免重复编译（PERF-01）
     * 3. 沙箱安全隔离（SEC-02）
     * 4. 类加载器生命周期管理
     */
    @Component
    public class GroovyScriptEngine {

        private static final Logger logger = LoggerFactory.getLogger(GroovyScriptEngine.class);

        private final ScriptCacheManager cacheManager;
        private final SecurityConfiguration securityConfig;
        private final ClassLoaderManager classLoaderManager;

        public GroovyScriptEngine(
            ScriptCacheManager cacheManager,
            SecurityConfiguration securityConfig,
            ClassLoaderManager classLoaderManager
        ) {
            this.cacheManager = cacheManager;
            this.securityConfig = securityConfig;
            this.classLoaderManager = classLoaderManager;
        }

        /**
         * 执行 Groovy 脚本
         * D-04: 使用 GroovyShell 动态编译和执行
         */
        public Object executeScript(String ruleId, String script, Map<String, Object> context) {
            try {
                // 计算脚本哈希值用于缓存键
                String scriptHash = calculateHash(script);

                // PERF-01: 从缓存获取已编译的脚本
                Class<?> scriptClass = cacheManager.get(ruleId, scriptHash);

                if (scriptClass == null) {
                    // 缓存未命中，编译脚本
                    scriptClass = compileScript(ruleId, script);
                    cacheManager.put(ruleId, scriptHash, scriptClass);
                    logger.debug("Compiled and cached script for rule: {}", ruleId);
                }

                // 执行脚本
                return executeScriptClass(scriptClass, context);

            } catch (Exception e) {
                logger.error("Failed to execute script for rule: {}", ruleId, e);
                throw new RuleExecutionException("脚本执行失败: " + e.getMessage(), e);
            }
        }

        /**
         * 编译 Groovy 脚本
         * D-04: 使用 GroovyShell 解析脚本
         */
        private Class<?> compileScript(String ruleId, String script) {
            try {
                groovy.lang.GroovyShell shell = new groovy.lang.GroovyShell(
                    classLoaderManager.getClassLoader(ruleId),
                    new Binding(),
                    securityConfig.createSecureConfiguration()
                );

                return shell.parse(script).getClass();

            } catch (Exception e) {
                logger.error("Failed to compile script for rule: {}", ruleId, e);
                throw new RuleExecutionException("脚本编译失败: " + e.getMessage(), e);
            }
        }

        /**
         * 执行已编译的脚本类
         */
        private Object executeScriptClass(Class<?> scriptClass, Map<String, Object> context) {
            try {
                // 创建脚本实例
                Script scriptInstance = (Script) scriptClass.getDeclaredConstructor().newInstance();

                // 绑定上下文变量
                Binding binding = new Binding(context);
                scriptInstance.setBinding(binding);

                // 执行脚本
                return scriptInstance.run();

            } catch (Exception e) {
                logger.error("Failed to run script instance", e);
                throw new RuleExecutionException("脚本运行失败: " + e.getMessage(), e);
            }
        }

        /**
         * 计算脚本哈希值（用于缓存键）
         */
        private String calculateHash(String script) {
            try {
                MessageDigest md = MessageDigest.getInstance("SHA-256");
                byte[] hash = md.digest(script.getBytes());
                StringBuilder hexString = new StringBuilder();
                for (byte b : hash) {
                    String hex = Integer.toHexString(0xff & b);
                    if (hex.length() == 1) hexString.append('0');
                    hexString.append(hex);
                }
                return hexString.toString();
            } catch (Exception e) {
                return String.valueOf(script.hashCode());
            }
        }
    }
    ```

    **src/main/java/com/example/ruleengine/exception/RuleExecutionException.java:**
    ```java
    package com.example.ruleengine.exception;

    public class RuleExecutionException extends RuntimeException {

        public RuleExecutionException(String message) {
            super(message);
        }

        public RuleExecutionException(String message, Throwable cause) {
            super(message, cause);
        }
    }
    ```

    关键实现要点：
    1. 使用 GroovyShell 动态编译和执行脚本（D-04）
    2. 使用 ScriptCacheManager 缓存已编译脚本（PERF-01）
    3. 使用 SecurityConfiguration 创建安全沙箱（SEC-02）
    4. 使用 ClassLoaderManager 管理 ClassLoader 生命周期（SEC-02）
    5. 计算脚本 SHA-256 哈希作为缓存键
  </action>
  <verify>
    <automated>./gradlew test --tests "*GroovyScriptEngineTest"</automated>
  </verify>
  <done>
    - GroovyScriptEngine 类已创建
    - 整合了缓存、安全和类加载器管理
    - 支持动态编译和执行 Groovy 脚本
    - 单元测试覆盖正常执行、缓存命中、异常处理场景
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/engine/GroovyScriptEngine.java exists
    - GroovyScriptEngine.java contains "public Object executeScript"
    - GroovyScriptEngine.java contains "ScriptCacheManager"
    - GroovyScriptEngine.java contains "SecurityConfiguration"
    - GroovyScriptEngine.java contains "ClassLoaderManager"
    - src/main/java/com/example/ruleengine/exception/RuleExecutionException.java exists
  </acceptance_criteria>
</task>

## 验证

完成上述任务后，运行以下验证步骤：

1. **单元测试验证:**
   ```bash
   ./gradlew test --tests "*GroovyScriptEngineTest"
   ./gradlew test --tests "*ScriptCacheManagerTest"
   ./gradlew test --tests "*ClassLoaderManagerTest"
   ```
   确保所有单元测试通过。

2. **集成验证:**
   编写一个简单的集成测试，验证端到端脚本执行：
   ```java
   @Test
   void testEndToEndScriptExecution() {
       String script = "def result = x + y; return result";
       Map<String, Object> context = Map.of("x", 10, "y", 20);

       Object result = groovyScriptEngine.executeScript("test-rule", script, context);

       assertEquals(30, result);
   }
   ```

3. **缓存验证:**
   ```bash
   # 检查缓存统计
   curl http://localhost:8080/actuator/metrics/script.cache.size
   ```

## 成功标准

1. Groovy 脚本可动态加载并执行
2. 脚本编译结果被缓存，避免重复编译
3. 沙箱安全配置禁用了危险类
4. 类加载器定期清理，防止内存泄漏
5. 所有单元测试通过，覆盖率 > 80%

## 输出

完成后创建 `.planning/phases/01-core-engine/01-02-SUMMARY.md`
