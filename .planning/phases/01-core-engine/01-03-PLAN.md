---
phase: 01-core-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - src/main/java/com/example/ruleengine/service/FeatureProviderService.java
  - src/main/java/com/example/ruleengine/model/FeatureRequest.java
  - src/main/java/com/example/ruleengine/model/FeatureResponse.java
  - src/test/java/com/example/ruleengine/service/FeatureProviderServiceTest.java
autonomous: true
requirements:
  - REXEC-04
  - PERF-02
user_setup: []

must_haves:
  truths:
    - "特征获取优先使用入参"
    - "缺失特征可降级到外部特征平台"
    - "外部特征获取超时时返回默认值"
    - "支持特征预加载和批量获取"
    - "所有单元测试通过"
  artifacts:
    - path: "src/main/java/com/example/ruleengine/service/FeatureProviderService.java"
      provides: "特征获取服务"
      contains: "public Map<String, Object> getFeatures"
      contains: "CompletableFuture"
    - path: "src/main/java/com/example/ruleengine/model/FeatureRequest.java"
      provides: "特征请求模型"
      contains: "private Map<String, Object> inputFeatures"
      contains: "private List<String> requiredFeatures"
    - path: "src/main/java/com/example/ruleengine/config/CaffeineCacheConfig.java"
      provides: "Caffeine 缓存配置"
      contains: "@Configuration"
      contains: "@Bean"
      contains: "Cache"
  key_links:
    - from: "FeatureProviderService.java"
      to: "Caffeine"
      via: "Spring Cache 抽象"
      pattern: "@Cacheable|@CacheEvict"
    - from: "FeatureProviderService.java"
      to: "外部特征平台"
      via: "RestTemplate 或 WebClient"
      pattern: "restTemplate|webClient"
    - from: "FeatureProviderService.java"
      to: "CompletableFuture"
      via: "超时控制"
      pattern: "CompletableFuture.*supplyAsync|get.*timeout"
---

## 目标

实现特征获取服务，支持"入参优先 → 外部降级 → 默认值"三级策略，确保特征获取不阻塞规则执行。

## 目的

建立灵活的特征获取机制，优先使用请求入参中的特征，缺失时从外部特征平台获取，超时或失败时使用默认值，确保规则执行的性能和稳定性。

## 输出

- 支持三级策略的特征获取服务
- Caffeine 缓存配置
- 特征预加载和批量获取能力
- 完整的单元测试覆盖

## 执行上下文

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## 上下文

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-engine/01-CONTEXT.md
@.planning/phases/01-core-engine/01-RESEARCH.md

## 接口定义

### FeatureProviderService 接口

```java
package com.example.ruleengine.service;

import java.util.List;
import java.util.Map;

/**
 * 特征提供者服务
 * 职责：获取规则执行所需的特征数据
 */
public interface FeatureProviderService {

    /**
     * 获取特征（三级策略：入参 → 外部 → 默认值）
     * @param inputFeatures 入参特征
     * @param requiredFeatures 必需的特征列表
     * @param timeoutMs 外部特征获取超时时间（毫秒）
     * @return 完整的特征数据
     */
    Map<String, Object> getFeatures(
        Map<String, Object> inputFeatures,
        List<String> requiredFeatures,
        long timeoutMs
    );

    /**
     * 预加载特征到缓存
     * @param featureKeys 要预加载的特征键列表
     */
    void preloadFeatures(List<String> featureKeys);

    /**
     * 批量获取特征
     * @param featureKeys 特征键列表
     * @param timeoutMs 超时时间（毫秒）
     * @return 特征数据
     */
    Map<String, Object> batchGetFeatures(List<String> featureKeys, long timeoutMs);
}
```

## 任务

<task type="auto">
  <name>Task 1: 实现 CaffeineCacheConfig 缓存配置</name>
  <files>
    src/main/java/com/example/ruleengine/config/CaffeineCacheConfig.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-RESEARCH.md（提取 Caffeine 缓存配置）
    src/main/resources/application.yml（查看缓存配置）
  </read_first>
  <action>
    创建 Caffeine 缓存配置类，基于 RESEARCH.md 代码示例：

    **src/main/java/com/example/ruleengine/config/CaffeineCacheConfig.java:**
    ```java
    package com.example.ruleengine.config;

    import com.github.benmanes.caffeine.cache.Cache;
    import com.github.benmanes.caffeine.cache.Caffeine;
    import org.springframework.cache.CacheManager;
    import.springframework.cache.annotation.EnableCaching;
    import org.springframework.cache.caffeine.CaffeineCacheManager;
    import.springframework.context.annotation.Bean;
    import org.springframework.context.annotation.Configuration;

    import java.util.concurrent.TimeUnit;

    /**
     * Caffeine 缓存配置
     * Source: RESEARCH.md 标准技术栈 + CONTEXT.md 决策 D-13
     *
     * 功能：
     * 1. 配置本地缓存用于高频特征
     * 2. 配置脚本缓存（已在 ScriptCacheManager 实现）
     * 3. 支持缓存统计和监控
     */
    @Configuration
    @EnableCaching
    public class CaffeineCacheConfig {

        /**
         * 特征缓存配置
         * D-13: 使用 Caffeine 实现本地缓存
         */
        @Bean
        public Cache<String, Object> featureCache() {
            return Caffeine.newBuilder()
                .maximumSize(10_000)  // 最大缓存 10000 个特征
                .expireAfterWrite(30, TimeUnit.MINUTES)  // 30 分钟过期
                .recordStats()  // 启用统计
                .build();
        }

        /**
         * Spring Cache 抽象的 CacheManager
         */
        @Bean
        public CacheManager cacheManager() {
            CaffeineCacheManager cacheManager = new CaffeineCacheManager();
            cacheManager.setCaffeine(Caffeine.newBuilder()
                .maximumSize(10_000)
                .expireAfterWrite(30, TimeUnit.MINUTES)
                .recordStats());
            return cacheManager;
        }
    }
    ```

    关键实现要点：
    1. 配置特征缓存，最大 10000 条，30 分钟过期
    2. 启用统计功能（recordStats）用于监控
    3. 提供 CacheManager 支持 Spring Cache 注解
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>
    - CaffeineCacheConfig 类已创建
    - 配置了特征缓存
    - 启用了 Spring Cache 注解支持
    - 编译成功，无错误
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/config/CaffeineCacheConfig.java exists
    - CaffeineCacheConfig.java contains "@Configuration"
    - CaffeineCacheConfig.java contains "@EnableCaching"
    - CaffeineCacheConfig.java contains "Caffeine.newBuilder()"
    - CaffeineCacheConfig.java contains "recordStats()"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 实现 FeatureProviderService 特征获取服务</name>
  <files>
    src/main/java/com/example/ruleengine/service/FeatureProviderService.java
    src/main/java/com/example/ruleengine/model/FeatureRequest.java
    src/main/java/com/example/ruleengine/model/FeatureResponse.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-RESEARCH.md（提取特征获取三级策略模式）
    src/main/java/com/example/ruleengine/config/CaffeineCacheConfig.java（理解缓存接口）
  </read_first>
  <action>
    创建特征获取服务，实现三级策略和批量获取：

    **src/main/java/com/example/ruleengine/model/FeatureRequest.java:**
    ```java
    package com.example.ruleengine.model;

import java.util.List;
    import java.util.Map;

    /**
     * 特征请求模型
     */
    public class FeatureRequest {

        private Map<String, Object> inputFeatures;
        private List<String> requiredFeatures;
        private long timeoutMs = 20;  // 默认 20ms 超时

        // 构造器
        public FeatureRequest() {}

        public FeatureRequest(Map<String, Object> inputFeatures, List<String> requiredFeatures) {
            this.inputFeatures = inputFeatures;
            this.requiredFeatures = requiredFeatures;
        }

        // Getters and Setters
        public Map<String, Object> getInputFeatures() {
            return inputFeatures;
        }

        public void setInputFeatures(Map<String, Object> inputFeatures) {
            this.inputFeatures = inputFeatures;
        }

        public List<String> getRequiredFeatures() {
            return requiredFeatures;
        }

        public void setRequiredFeatures(List<String> requiredFeatures) {
            this.requiredFeatures = requiredFeatures;
        }

        public long getTimeoutMs() {
            return timeoutMs;
        }

        public void setTimeoutMs(long timeoutMs) {
            this.timeoutMs = timeoutMs;
        }
    }
    ```

    **src/main/java/com/example/ruleengine/model/FeatureResponse.java:**
    ```java
    package com.example.ruleengine.model;

    import java.util.Map;

    /**
     * 特征响应模型
     */
    public class FeatureResponse {

        private Map<String, Object> features;
        private boolean cacheHit;
        private boolean fallbackToDefault;
        private long fetchTimeMs;

        // 构造器
        public FeatureResponse() {}

        public FeatureResponse(Map<String, Object> features) {
            this.features = features;
        }

        // Getters and Setters
        public Map<String, Object> getFeatures() {
            return features;
        }

        public void setFeatures(Map<String, Object> features) {
            this.features = features;
        }

        public boolean isCacheHit() {
            return cacheHit;
        }

        public void setCacheHit(boolean cacheHit) {
            this.cacheHit = cacheHit;
        }

        public boolean isFallbackToDefault() {
            return fallbackToDefault;
        }

        public void setFallbackToDefault(boolean fallbackToDefault) {
            this.fallbackToDefault = fallbackToDefault;
        }

        public long getFetchTimeMs() {
            return fetchTimeMs;
        }

        public void setFetchTimeMs(long fetchTimeMs) {
            this.fetchTimeMs = fetchTimeMs;
        }
    }
    ```

    **src/main/java/com/example/ruleengine/service/FeatureProviderService.java:**
    ```java
    package com.example.ruleengine.service;

    import com.github.benmanes.caffeine.cache.Cache;
    import com.example.ruleengine.model.FeatureRequest;
    import com.example.ruleengine.model.FeatureResponse;
    import org.slf4j.Logger;
    import org.slf4j.LoggerFactory;
    import org.springframework.beans.factory.annotation.Qualifier;
    import org.springframework.stereotype.Service;
    import org.springframework.web.client.RestTemplate;

    import java.util.*;
    import java.util.concurrent.CompletableFuture;
    import java.util.concurrent.TimeUnit;
    import java.util.stream.Collectors;

    /**
     * 特征提供者服务
     * Source: RESEARCH.md 模式4 + CONTEXT.md 决策 D-14, D-15
     *
     * 功能：
     * 1. D-14: 三级策略（入参 → 外部 → 默认值）
     * 2. D-15: 超时控制和降级
     * 3. PERF-02: 特征预加载和批量获取
     */
    @Service
    public class FeatureProviderService {

        private static final Logger logger = LoggerFactory.getLogger(FeatureProviderService.class);

        private final Cache<String, Object> featureCache;
        private final RestTemplate restTemplate;
        private final Map<String, Object> defaultFeatures;

        public FeatureProviderService(
            @Qualifier("featureCache") Cache<String, Object> featureCache,
            RestTemplate restTemplate
        ) {
            this.featureCache = featureCache;
            this.restTemplate = restTemplate;
            this.defaultFeatures = loadDefaultFeatures();
        }

        /**
         * 获取特征（三级策略）
         * D-14: 入参优先 → 外部降级 → 默认值
         */
        public FeatureResponse getFeatures(FeatureRequest request) {
            long startTime = System.currentTimeMillis();
            Map<String, Object> result = new HashMap<>(request.getInputFeatures());
            boolean fallbackToDefault = false;

            // 找出缺失的特征
            List<String> missingFeatures = request.getRequiredFeatures().stream()
                .filter(feature -> !request.getInputFeatures().containsKey(feature))
                .collect(Collectors.toList());

            if (!missingFeatures.isEmpty()) {
                // 从缓存获取
                Map<String, Object> cachedFeatures = getFeaturesFromCache(missingFeatures);
                result.putAll(cachedFeatures);

                // 仍然缺失的特征
                List<String> stillMissing = missingFeatures.stream()
                    .filter(feature -> !cachedFeatures.containsKey(feature))
                    .collect(Collectors.toList());

                if (!stillMissing.isEmpty()) {
                    // D-15: 从外部特征平台获取（带超时控制）
                    Map<String, Object> externalFeatures = fetchExternalFeaturesWithTimeout(
                        stillMissing,
                        request.getTimeoutMs()
                    );
                    result.putAll(externalFeatures);

                    // 仍然缺失的特征，使用默认值
                    List<String> finalMissing = stillMissing.stream()
                        .filter(feature -> !externalFeatures.containsKey(feature))
                        .collect(Collectors.toList());

                    if (!finalMissing.isEmpty()) {
                        finalMissing.forEach(feature ->
                            result.put(feature, defaultFeatures.getOrDefault(feature, null))
                        );
                        fallbackToDefault = true;
                        logger.debug("Used default values for features: {}", finalMissing);
                    }
                }
            }

            long fetchTime = System.currentTimeMillis() - startTime;

            FeatureResponse response = new FeatureResponse();
            response.setFeatures(result);
            response.setCacheHit(false);  // TODO: 实现缓存命中检测
            response.setFallbackToDefault(fallbackToDefault);
            response.setFetchTimeMs(fetchTime);

            return response;
        }

        /**
         * 从缓存获取特征
         */
        private Map<String, Object> getFeaturesFromCache(List<String> featureKeys) {
            Map<String, Object> result = new HashMap<>();
            for (String key : featureKeys) {
                Object value = featureCache.getIfPresent(key);
                if (value != null) {
                    result.put(key, value);
                    logger.debug("Cache hit for feature: {}", key);
                }
            }
            return result;
        }

        /**
         * 从外部特征平台获取特征（带超时控制）
         * D-15: 使用 CompletableFuture.anyOf() 实现超时控制
         */
        private Map<String, Object> fetchExternalFeaturesWithTimeout(
            List<String> featureKeys,
            long timeoutMs
        ) {
            try {
                CompletableFuture<Map<String, Object>> externalFuture =
                    CompletableFuture.supplyAsync(() -> fetchExternalFeatures(featureKeys));

                // 超时控制
                Map<String, Object> result = externalFuture.get(timeoutMs, TimeUnit.MILLISECONDS);

                // 将获取到的特征放入缓存
                result.forEach((key, value) -> featureCache.put(key, value));

                return result;

            } catch (Exception e) {
                logger.warn("Failed to fetch external features: {}", featureKeys, e);
                return Collections.emptyMap();  // 失败返回空，触发默认值降级
            }
        }

        /**
         * 调用外部特征平台
         */
        private Map<String, Object> fetchExternalFeatures(List<String> featureKeys) {
            try {
                // TODO: 实现实际的 HTTP 调用
                // 当前返回模拟数据
                return restTemplate.postForObject(
                    "http://feature-platform/api/features",
                    featureKeys,
                    Map.class
                );
            } catch (Exception e) {
                logger.error("Failed to call external feature platform", e);
                return Collections.emptyMap();
            }
        }

        /**
         * PERF-02: 预加载特征到缓存
         */
        public void preloadFeatures(List<String> featureKeys) {
            logger.info("Preloading features: {}", featureKeys);

            CompletableFuture.runAsync(() -> {
                Map<String, Object> features = fetchExternalFeatures(featureKeys);
                features.forEach((key, value) -> featureCache.put(key, value));
                logger.info("Preloaded {} features", features.size());
            });
        }

        /**
         * PERF-02: 批量获取特征
         */
        public Map<String, Object> batchGetFeatures(List<String> featureKeys, long timeoutMs) {
            return fetchExternalFeaturesWithTimeout(featureKeys, timeoutMs);
        }

        /**
         * 加载默认特征值
         */
        private Map<String, Object> loadDefaultFeatures() {
            Map<String, Object> defaults = new HashMap<>();
            defaults.put("user_age", 0);
            defaults.put("user_level", "NORMAL");
            defaults.put("order_amount", 0.0);
            defaults.put("risk_score", 0.5);
            // TODO: 从配置文件读取默认值
            return defaults;
        }
    }
    ```

    关键实现要点：
    1. 实现三级策略：入参 → 缓存 → 外部 → 默认值（D-14）
    2. 使用 CompletableFuture 实现超时控制（D-15）
    3. 支持特征预加载和批量获取（PERF-02）
    4. 使用 Caffeine 缓存高频特征
  </action>
  <verify>
    <automated>./gradlew test --tests "*FeatureProviderServiceTest"</automated>
  </verify>
  <done>
    - FeatureProviderService 类已创建
    - 实现了三级策略特征获取
    - 实现了超时控制和降级
    - 实现了特征预加载和批量获取
    - 单元测试覆盖所有策略分支
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/service/FeatureProviderService.java exists
    - FeatureProviderService.java contains "public FeatureResponse getFeatures"
    - FeatureProviderService.java contains "CompletableFuture"
    - FeatureProviderService.java contains "fetchExternalFeaturesWithTimeout"
    - src/main/java/com/example/ruleengine/model/FeatureRequest.java exists
    - src/main/java/com/example/ruleengine/model/FeatureResponse.java exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 配置 RestTemplate Bean</name>
  <files>
    src/main/java/com/example/ruleengine/config/RestTemplateConfig.java
  </files>
  <read_first>
    src/main/java/com/example/ruleengine/service/FeatureProviderService.java（理解 RestTemplate 使用）
  </read_first>
  <action>
    创建 RestTemplate 配置类：

    **src/main/java/com/example/ruleengine/config/RestTemplateConfig.java:**
    ```java
    package com.example.ruleengine.config;

    import org.springframework.context.annotation.Bean;
    import org.springframework.context.annotation.Configuration;
    import org.springframework.web.client.RestTemplate;

    /**
     * RestTemplate 配置
     */
    @Configuration
    public class RestTemplateConfig {

        @Bean
        public RestTemplate restTemplate() {
            return new RestTemplate();
        }
    }
    ```
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>
    - RestTemplateConfig 类已创建
    - RestTemplate Bean 已配置
    - 编译成功，无错误
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/config/RestTemplateConfig.java exists
    - RestTemplateConfig.java contains "@Bean"
    - RestTemplateConfig.java contains "public RestTemplate restTemplate()"
  </acceptance_criteria>
</task>

## 验证

完成上述任务后，运行以下验证步骤：

1. **单元测试验证:**
   ```bash
   ./gradlew test --tests "*FeatureProviderServiceTest"
   ```
   确保所有单元测试通过。

2. **缓存验证:**
   ```bash
   # 检查缓存统计
   curl http://localhost:8080/actuator/metrics/cache.gets
   curl http://localhost:8080/actuator/metrics/cache.hits
   ```

3. **性能验证:**
   编写性能测试，验证特征获取耗时：
   ```java
   @Test
   void testFeatureFetchPerformance() {
       FeatureRequest request = new FeatureRequest(Map.of(), List.of("user_age", "user_level"));
       request.setTimeoutMs(20);

       long startTime = System.nanoTime();
       FeatureResponse response = featureProviderService.getFeatures(request);
       long duration = (System.nanoTime() - startTime) / 1_000_000;  // 转换为毫秒

       assertTrue(duration < 25, "Feature fetch should complete within 25ms, actual: " + duration);
   }
   ```

## 成功标准

1. 特征获取支持三级策略（入参 → 外部 → 默认值）
2. 外部特征获取超时时间可控（默认 20ms）
3. 特征缓存命中率高（> 80%）
4. 特征预加载和批量获取功能正常
5. 所有单元测试通过，覆盖率 > 80%

## 输出

完成后创建 `.planning/phases/01-core-engine/01-03-SUMMARY.md`
