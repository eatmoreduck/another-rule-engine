---
phase: 01-core-engine
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/main/java/com/example/ruleengine/service/RuleExecutionService.java
  - src/main/java/com/example/ruleengine/config/Resilience4jConfig.java
  - src/test/java/com/example/ruleengine/service/RuleExecutionServiceTest.java
autonomous: true
requirements:
  - REXEC-01
user_setup: []

must_haves:
  truths:
    - "规则执行使用 Resilience4j TimeLimiter 控制超时"
    - "规则执行超时时间设置为 50ms"
    - "超时后返回降级决策（REJECT）"
    - "规则执行在独立线程池中运行"
    - "所有单元测试通过"
  artifacts:
    - path: "src/main/java/com/example/ruleengine/service/RuleExecutionService.java"
      provides: "规则执行服务"
      contains: "public DecisionResponse executeDecision"
      contains: "TimeLimiter"
      contains: "ThreadPoolTaskExecutor"
    - path: "src/main/java/com/example/ruleengine/config/Resilience4jConfig.java"
      provides: "Resilience4j 配置"
      contains: "@Configuration"
      contains: "TimeLimiterConfig"
      contains: "ThreadPoolTaskExecutor"
    - path: "src/main/java/com/example/ruleengine/model/DecisionRequest.java"
      provides: "决策请求模型"
      contains: "private String ruleId"
      contains: "private Map<String, Object> features"
    - path: "src/main/java/com/example/ruleengine/model/DecisionResponse.java"
      provides: "决策响应模型"
      contains: "private String decision"
      contains: "private String reason"
      contains: "private long executionTimeMs"
  key_links:
    - from: "RuleExecutionService.java"
      to: "GroovyScriptEngine"
      via: "依赖注入"
      pattern: "private.*GroovyScriptEngine"
    - from: "RuleExecutionService.java"
      to: "FeatureProviderService"
      via: "依赖注入"
      pattern: "private.*FeatureProviderService"
    - from: "RuleExecutionService.java"
      to: "Resilience4j TimeLimiter"
      via: "超时控制"
      pattern: "TimeLimiter.*decorateSupplier"
    - from: "RuleExecutionService.java"
      to: "独立线程池"
      via: "D-12: 线程池隔离"
      pattern: "CompletableFuture.*supplyAsync.*ruleExecutorPool"
    - from: "Resilience4jConfig.java"
      to: "ThreadPoolTaskExecutor"
      via: "Bean 配置"
      pattern: "@Bean.*ThreadPoolTaskExecutor"
---

## 目标

实现规则执行服务，使用 Resilience4j TimeLimiter 控制 50ms 超时，使用独立线程池执行规则隔离风险（D-12），整合 Groovy 脚本引擎和特征获取服务。

## 目的

建立规则执行的核心业务逻辑，确保规则在 50ms 内完成执行，超时后自动降级，防止单个规则影响整体性能。使用独立线程池执行规则，隔离规则执行风险，防止规则异常影响主线程池。

## 输出

- 支持超时控制的规则执行服务
- Resilience4j 配置（TimeLimiter + ThreadPoolBulkhead）
- 独立线程池配置（D-12）
- 决策请求/响应模型
- 完整的单元测试覆盖

## 执行上下文

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## 上下文

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-engine/01-CONTEXT.md
@.planning/phases/01-core-engine/01-RESEARCH.md

## 接口定义

### RuleExecutionService 接口

```java
package com.example.ruleengine.service;

import com.example.ruleengine.model.DecisionRequest;
import com.example.ruleengine.model.DecisionResponse;

/**
 * 规则执行服务
 * 职责：执行规则并返回决策结果
 */
public interface RuleExecutionService {

    /**
     * 执行规则决策
     * @param request 决策请求
     * @return 决策响应
     */
    DecisionResponse executeDecision(DecisionRequest request);
}
```

### DecisionRequest 模型

```java
package com.example.ruleengine.model;

import java.util.Map;

/**
 * 决策请求模型
 */
public class DecisionRequest {

    private String ruleId;
    private String script;  // Groovy 脚本内容
    private Map<String, Object> features;
    private List<String> requiredFeatures;  // 必需的特征列表
    private long timeoutMs = 50;  // 默认 50ms 超时

    // Getters and Setters
    public String getRuleId() { return ruleId; }
    public void setRuleId(String ruleId) { this.ruleId = ruleId; }

    public String getScript() { return script; }
    public void setScript(String script) { this.script = script; }

    public Map<String, Object> getFeatures() { return features; }
    public void setFeatures(Map<String, Object> features) { this.features = features; }

    public List<String> getRequiredFeatures() { return requiredFeatures; }
    public void setRequiredFeatures(List<String> requiredFeatures) { this.requiredFeatures = requiredFeatures; }

    public long getTimeoutMs() { return timeoutMs; }
    public void setTimeoutMs(long timeoutMs) { this.timeoutMs = timeoutMs; }
}
```

### DecisionResponse 模型

```java
package com.example.ruleengine.model;

/**
 * 决策响应模型
 */
public class DecisionResponse {

    private String decision;  // "PASS" 或 "REJECT"
    private String reason;
    private long executionTimeMs;
    private boolean timeout;
    private Map<String, Object> executionContext;

    // 构造器
    public DecisionResponse() {}

    public DecisionResponse(String decision, String reason, long executionTimeMs) {
        this.decision = decision;
        this.reason = reason;
        this.executionTimeMs = executionTimeMs;
    }

    // Getters and Setters
    public String getDecision() { return decision; }
    public void setDecision(String decision) { this.decision = decision; }

    public String getReason() { return reason; }
    public void setReason(String reason) { this.reason = reason; }

    public long getExecutionTimeMs() { return executionTimeMs; }
    public void setExecutionTimeMs(long executionTimeMs) { this.executionTimeMs = executionTimeMs; }

    public boolean isTimeout() { return timeout; }
    public void setTimeout(boolean timeout) { this.timeout = timeout; }

    public Map<String, Object> getExecutionContext() { return executionContext; }
    public void setExecutionContext(Map<String, Object> executionContext) { this.executionContext = executionContext; }

    // Builder 模式
    public static Builder builder() {
        return new Builder();
    }

    public static class Builder {
        private DecisionResponse response = new DecisionResponse();

        public Builder decision(String decision) {
            response.decision = decision;
            return this;
        }

        public Builder reason(String reason) {
            response.reason = reason;
            return this;
        }

        public Builder executionTimeMs(long executionTimeMs) {
            response.executionTimeMs = executionTimeMs;
            return this;
        }

        public Builder timeout(boolean timeout) {
            response.timeout = timeout;
            return this;
        }

        public DecisionResponse build() {
            return response;
        }
    }
}
```

## 任务

<task type="auto">
  <name>Task 1: 实现 Resilience4jConfig 配置类和线程池配置</name>
  <files>
    src/main/java/com/example/ruleengine/config/Resilience4jConfig.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-RESEARCH.md（提取 Resilience4j 配置模式）
  </read_first>
  <action>
    创建 Resilience4j 配置类和独立线程池配置，基于 RESEARCH.md 模式3 + CONTEXT.md 决策 D-11、D-12：

    **src/main/java/com/example/ruleengine/config/Resilience4jConfig.java:**
    ```java
    package com.example.ruleengine.config;

    import io.github.resilience4j.timelimiter.TimeLimiter;
    import io.github.resilience4j.timelimiter.TimeLimiterConfig;
    import org.springframework.context.annotation.Bean;
    import org.springframework.context.annotation.Configuration;
    import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;

    import java.time.Duration;
    import java.util.concurrent.ThreadPoolExecutor;

    /**
     * Resilience4j 配置
     * Source: RESEARCH.md 模式3 + CONTEXT.md 决策 D-11、D-12
     *
     * 功能：
     * 1. D-11: 配置 50ms 超时控制
     * 2. D-12: 配置独立线程池执行规则，隔离风险
     * 3. 防止单个规则执行影响整体性能
     */
    @Configuration
    public class Resilience4jConfig {

        /**
         * 规则执行 TimeLimiter
         * D-11: 50ms 超时配置
         */
        @Bean
        public TimeLimiter ruleExecutionTimeLimiter() {
            TimeLimiterConfig config = TimeLimiterConfig.custom()
                .timeoutDuration(Duration.ofMillis(50))
                .build();

            return TimeLimiter.of(config);
        }

        /**
         * 规则执行独立线程池
         * D-12: 使用独立线程池执行规则，隔离风险
         *
         * 配置说明：
         * - 核心线程数：CPU核心数 * 2（充分利用多核）
         * - 最大线程数：核心线程数 * 2（突发流量处理）
         * - 队列容量：100（缓冲等待任务）
         * - 线程名前缀：rule-executor-（便于日志追踪）
         * - 拒绝策略：CallerRunsPolicy（确保不会抛出拒绝异常，由调用线程执行）
         */
        @Bean(name = "ruleExecutorPool")
        public ThreadPoolTaskExecutor ruleExecutorPool() {
            int corePoolSize = Runtime.getRuntime().availableProcessors() * 2;
            int maxPoolSize = corePoolSize * 2;

            ThreadPoolTaskExecutor executor = new ThreadPoolTaskExecutor();
            executor.setCorePoolSize(corePoolSize);
            executor.setMaxPoolSize(maxPoolSize);
            executor.setQueueCapacity(100);
            executor.setThreadNamePrefix("rule-executor-");
            executor.setRejectedExecutionHandler(new ThreadPoolExecutor.CallerRunsPolicy());
            executor.setWaitForTasksToCompleteOnShutdown(true);
            executor.setAwaitTerminationSeconds(60);
            executor.initialize();

            return executor;
        }
    }
    ```

    关键实现要点：
    1. 配置 50ms 超时（D-11）
    2. 创建独立线程池 ThreadPoolTaskExecutor（D-12）
    3. 核心线程数 = CPU核心数 * 2，最大线程数 = 核心线程数 * 2
    4. 使用 CallerRunsPolicy 拒绝策略，确保不会抛出拒绝异常
    5. TimeLimiter 实现超时控制
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>
    - Resilience4jConfig 类已创建
    - TimeLimiter Bean 已配置（D-11）
    - ThreadPoolTaskExecutor Bean 已配置（D-12）
    - 超时时间设置为 50ms
    - 编译成功，无错误
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/config/Resilience4jConfig.java exists
    - Resilience4jConfig.java contains "@Configuration"
    - Resilience4jConfig.java contains "TimeLimiterConfig.custom()"
    - Resilience4jConfig.java contains "Duration.ofMillis(50)"
    - Resilience4jConfig.java contains "@Bean(name = \"ruleExecutorPool\")"
    - Resilience4jConfig.java contains "ThreadPoolTaskExecutor"
    - Resilience4jConfig.java contains "CallerRunsPolicy"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 实现 DecisionRequest 和 DecisionResponse 模型</name>
  <files>
    src/main/java/com/example/ruleengine/model/DecisionRequest.java
    src/main/java/com/example/ruleengine/model/DecisionResponse.java
  </files>
  <read_first>
    无（新文件创建）
  </read_first>
  <action>
    创建决策请求和响应模型类：

    **src/main/java/com/example/ruleengine/model/DecisionRequest.java:**
    ```java
    package com.example.ruleengine.model;

    import java.util.List;
    import java.util.Map;

    /**
     * 决策请求模型
     * Source: CONTEXT.md 决策 D-17
     */
    public class DecisionRequest {

        private String ruleId;
        private String script;
        private Map<String, Object> features;
        private List<String> requiredFeatures;
        private long timeoutMs = 50;  // D-11: 默认 50ms 超时

        // 构造器
        public DecisionRequest() {}

        public DecisionRequest(String ruleId, String script, Map<String, Object> features) {
            this.ruleId = ruleId;
            this.script = script;
            this.features = features;
        }

        // Getters and Setters
        public String getRuleId() {
            return ruleId;
        }

        public void setRuleId(String ruleId) {
            this.ruleId = ruleId;
        }

        public String getScript() {
            return script;
        }

        public void setScript(String script) {
            this.script = script;
        }

        public Map<String, Object> getFeatures() {
            return features;
        }

        public void setFeatures(Map<String, Object> features) {
            this.features = features;
        }

        public List<String> getRequiredFeatures() {
            return requiredFeatures;
        }

        public void setRequiredFeatures(List<String> requiredFeatures) {
            this.requiredFeatures = requiredFeatures;
        }

        public long getTimeoutMs() {
            return timeoutMs;
        }

        public void setTimeoutMs(long timeoutMs) {
            this.timeoutMs = timeoutMs;
        }
    }
    ```

    **src/main/java/com/example/ruleengine/model/DecisionResponse.java:**
    （使用前面接口定义中的完整实现）
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <done>
    - DecisionRequest 类已创建
    - DecisionResponse 类已创建
    - 包含所有必需字段
    - 编译成功，无错误
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/model/DecisionRequest.java exists
    - DecisionRequest.java contains "private String ruleId"
    - DecisionRequest.java contains "private Map<String, Object> features"
    - src/main/java/com/example/ruleengine/model/DecisionResponse.java exists
    - DecisionResponse.java contains "private String decision"
    - DecisionResponse.java contains "private long executionTimeMs"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 实现 RuleExecutionService 规则执行服务（使用独立线程池）</name>
  <files>
    src/main/java/com/example/ruleengine/service/RuleExecutionService.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-RESEARCH.md（提取规则执行模式）
    src/main/java/com/example/ruleengine/engine/GroovyScriptEngine.java（理解脚本执行接口）
    src/main/java/com/example/ruleengine/service/FeatureProviderService.java（理解特征获取接口）
    src/main/java/com/example/ruleengine/config/Resilience4jConfig.java（理解 TimeLimiter 和线程池配置）
  </read_first>
  <action>
    创建规则执行服务，整合脚本执行和特征获取，使用独立线程池执行规则（D-12），基于 RESEARCH.md 模式3：

    **src/main/java/com/example/ruleengine/service/RuleExecutionService.java:**
    ```java
    package com.example.ruleengine.service;

    import com.example.ruleengine.engine.GroovyScriptEngine;
    import com.example.ruleengine.exception.RuleExecutionException;
    import com.example.ruleengine.model.DecisionRequest;
    import com.example.ruleengine.model.DecisionResponse;
    import com.example.ruleengine.model.FeatureRequest;
    import com.example.ruleengine.model.FeatureResponse;
    import io.github.resilience4j.timelimiter.TimeLimiter;
    import org.slf4j.Logger;
    import org.slf4j.LoggerFactory;
    import org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor;
    import org.springframework.stereotype.Service;

    import java.util.HashMap;
    import java.util.Map;
    import java.util.concurrent.CompletableFuture;
    import java.util.concurrent.TimeoutException;
    import java.util.function.Supplier;

    /**
     * 规则执行服务
     * Source: RESEARCH.md 模式3 + CONTEXT.md 决策 D-11、D-12
     *
     * 功能：
     * 1. REXEC-01: 同步 API 50ms 内返回决策结果
     * 2. 整合 Groovy 脚本引擎和特征获取服务
     * 3. D-12: 使用独立线程池执行规则，隔离风险
     */
    @Service
    public class RuleExecutionService {

        private static final Logger logger = LoggerFactory.getLogger(RuleExecutionService.class);

        private final GroovyScriptEngine scriptEngine;
        private final FeatureProviderService featureProvider;
        private final TimeLimiter timeLimiter;
        private final ThreadPoolTaskExecutor ruleExecutorPool;  // D-12: 独立线程池

        public RuleExecutionService(
            GroovyScriptEngine scriptEngine,
            FeatureProviderService featureProvider,
            TimeLimiter ruleExecutionTimeLimiter,
            ThreadPoolTaskExecutor ruleExecutorPool  // D-12: 注入独立线程池
        ) {
            this.scriptEngine = scriptEngine;
            this.featureProvider = featureProvider;
            this.timeLimiter = ruleExecutionTimeLimiter;
            this.ruleExecutorPool = ruleExecutorPool;  // D-12: 初始化线程池
        }

        /**
         * 执行规则决策
         * REXEC-01: 同步 API 接收决策请求，50ms 内返回结果
         * D-12: 使用独立线程池执行规则，隔离风险
         */
        public DecisionResponse executeDecision(DecisionRequest request) {
            long startTime = System.currentTimeMillis();

            try {
                // D-12: 使用独立线程池执行规则，隔离风险
                // CompletableFuture.supplyAsync 将规则执行提交到独立线程池
                Supplier<DecisionResponse> supplier = CompletableFuture.supplyAsync(
                    () -> executeRuleInternal(request),
                    ruleExecutorPool  // D-12: 指定使用独立线程池
                )::get;

                // 使用 Resilience4j TimeLimiter 包装执行，实现超时控制（D-11）
                Supplier<DecisionResponse> decoratedSupplier = TimeLimiter.decorateSupplier(
                    timeLimiter,
                    supplier
                );

                DecisionResponse response = decoratedSupplier.get();
                response.setExecutionTimeMs(System.currentTimeMillis() - startTime);

                logger.debug("Rule {} executed in {}ms", request.getRuleId(), response.getExecutionTimeMs());
                return response;

            } catch (TimeoutException e) {
                // D-11: 超时返回降级决策
                long executionTime = System.currentTimeMillis() - startTime;
                logger.warn("Rule {} execution timeout after {}ms", request.getRuleId(), executionTime);

                return DecisionResponse.builder()
                    .decision("REJECT")
                    .reason("规则执行超时")
                    .executionTimeMs(executionTime)
                    .timeout(true)
                    .build();

            } catch (Exception e) {
                // D-11: 异常返回降级决策
                long executionTime = System.currentTimeMillis() - startTime;
                logger.error("Rule {} execution failed after {}ms", request.getRuleId(), executionTime, e);

                return DecisionResponse.builder()
                    .decision("REJECT")
                    .reason("规则执行失败: " + e.getMessage())
                    .executionTimeMs(executionTime)
                    .timeout(false)
                    .build();
            }
        }

        /**
         * 内部规则执行逻辑（在独立线程池中执行）
         */
        private DecisionResponse executeRuleInternal(DecisionRequest request) {
            try {
                // 1. 获取特征（REXEC-04: 三级策略）
                FeatureRequest featureRequest = new FeatureRequest(
                    request.getFeatures(),
                    request.getRequiredFeatures()
                );
                featureRequest.setTimeoutMs(Math.min(20, request.getTimeoutMs())); // 特征获取最多 20ms

                FeatureResponse featureResponse = featureProvider.getFeatures(featureRequest);
                Map<String, Object> features = featureResponse.getFeatures();

                // 2. 执行 Groovy 脚本（REXEC-03: Groovy DSL 动态加载执行）
                Object result = scriptEngine.executeScript(
                    request.getRuleId(),
                    request.getScript(),
                    features
                );

                // 3. 构建决策响应
                return buildDecisionResponse(result, features);

            } catch (RuleExecutionException e) {
                logger.error("Rule execution failed for rule: {}", request.getRuleId(), e);
                throw e;  // 重新抛出，由外层处理
            }
        }

        /**
         * 构建决策响应
         * D-18: 响应格式 {decision, reason, executionTimeMs}
         */
        private DecisionResponse buildDecisionResponse(Object result, Map<String, Object> context) {
            DecisionResponse response = new DecisionResponse();

            // 解析脚本执行结果
            if (result instanceof Boolean) {
                response.setDecision(((Boolean) result) ? "PASS" : "REJECT");
                response.setReason("规则执行完成");
            } else if (result instanceof Map) {
                Map<?, ?> resultMap = (Map<?, ?>) result;
                response.setDecision((String) resultMap.getOrDefault("decision", "REJECT"));
                response.setReason((String) resultMap.getOrDefault("reason", "规则执行完成"));
            } else if (result instanceof String) {
                response.setDecision((String) result);
                response.setReason("规则执行完成");
            } else {
                response.setDecision("REJECT");
                response.setReason("规则返回无效结果: " + result);
            }

            response.setExecutionContext(new HashMap<>(context));

            return response;
        }
    }
    ```

    关键实现要点：
    1. 使用 TimeLimiter 实现超时控制（D-11）
    2. D-12: 注入 ThreadPoolTaskExecutor (ruleExecutorPool)
    3. D-12: 使用 CompletableFuture.supplyAsync 将规则执行提交到独立线程池
    4. D-12: 独立线程池隔离规则执行风险，防止规则异常影响主线程池
    5. 超时或异常时返回降级决策 REJECT
    6. 整合 Groovy 脚本引擎和特征获取服务
    7. 支持多种脚本返回格式（Boolean、Map、String）
  </action>
  <verify>
    <automated>./gradlew test --tests "*RuleExecutionServiceTest"</automated>
  </verify>
  <done>
    - RuleExecutionService 类已创建
    - 整合了脚本执行和特征获取
    - 实现了超时控制和降级（D-11）
    - 实现了独立线程池执行规则（D-12）
    - 单元测试覆盖正常执行、超时、异常场景
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/service/RuleExecutionService.java exists
    - RuleExecutionService.java contains "public DecisionResponse executeDecision"
    - RuleExecutionService.java contains "TimeLimiter.decorateSupplier"
    - RuleExecutionService.java contains "ThreadPoolTaskExecutor ruleExecutorPool"
    - RuleExecutionService.java contains "CompletableFuture.supplyAsync"
    - RuleExecutionService.java contains "GroovyScriptEngine"
    - RuleExecutionService.java contains "FeatureProviderService"
  </acceptance_criteria>
</task>

## 验证

完成上述任务后，运行以下验证步骤：

1. **单元测试验证:**
   ```bash
   ./gradlew test --tests "*RuleExecutionServiceTest"
   ```
   确保所有单元测试通过。

2. **超时控制验证:**
   编写测试验证超时降级：
   ```java
   @Test
   void testRuleExecutionTimeout() {
       String slowScript = "Thread.sleep(100); return true";
       DecisionRequest request = new DecisionRequest("timeout-rule", slowScript, Map.of());
       request.setTimeoutMs(50);

       DecisionResponse response = ruleExecutionService.executeDecision(request);

       assertEquals("REJECT", response.getDecision());
       assertTrue(response.isTimeout());
       assertTrue(response.getExecutionTimeMs() < 60); // 应该在 60ms 内返回
   }
   ```

3. **线程池隔离验证（D-12）:**
   编写测试验证线程池隔离：
   ```java
   @Test
   void testRuleExecutionThreadPoolIsolation() throws Exception {
       // 创建多个并发规则执行请求
       List<CompletableFuture<DecisionResponse>> futures = new ArrayList<>();
       for (int i = 0; i < 20; i++) {
           String script = "Thread.sleep(10); return amount > 1000";
           DecisionRequest request = new DecisionRequest(
               "thread-test-rule-" + i,
               script,
               Map.of("amount", 500 + i * 100)
           );
           futures.add(CompletableFuture.supplyAsync(() ->
               ruleExecutionService.executeDecision(request)
           ));
       }

       // 等待所有请求完成
       CompletableFuture.allOf(futures.toArray(new CompletableFuture[0])).get();

       // 验证所有请求都成功完成
       for (CompletableFuture<DecisionResponse> future : futures) {
           DecisionResponse response = future.get();
           assertNotNull(response.getDecision());
           assertTrue(response.getExecutionTimeMs() < 100);
       }
   }
   ```

4. **性能验证:**
   编写性能测试，验证 50ms 目标：
   ```java
   @Test
   void testRuleExecutionPerformance() {
       String simpleScript = "def result = amount > 1000; return ['decision': result ? 'REJECT' : 'PASS']";
       DecisionRequest request = new DecisionRequest(
           "perf-rule",
           simpleScript,
           Map.of("amount", 500)
       );

       long startTime = System.nanoTime();
       DecisionResponse response = ruleExecutionService.executeDecision(request);
       long duration = (System.nanoTime() - startTime) / 1_000_000;

       assertEquals("PASS", response.getDecision());
       assertTrue(duration < 50, "Rule execution should complete within 50ms, actual: " + duration);
   }
   ```

## 成功标准

1. 规则执行支持 50ms 超时控制（D-11）
2. 超时后自动返回降级决策（REJECT）
3. 使用独立线程池执行规则，隔离风险（D-12）
4. 整合了 Groovy 脚本引擎和特征获取服务
5. 支持多种脚本返回格式
6. 所有单元测试通过，覆盖率 > 80%

## 输出

完成后创建 `.planning/phases/01-core-engine/01-04-SUMMARY.md`
