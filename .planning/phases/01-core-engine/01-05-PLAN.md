---
phase: 01-core-engine
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - src/main/java/com/example/ruleengine/controller/DecisionController.java
  - src/main/java/com/example/ruleengine/config/RestTemplateConfig.java
  - src/test/java/com/example/ruleengine/controller/DecisionControllerTest.java
  - src/test/java/com/example/ruleengine/integration/DecisionControllerIntegrationTest.java
autonomous: true
requirements:
  - REXEC-01
user_setup: []

must_haves:
  truths:
    - "POST /api/v1/decide 端点可访问"
    - "请求格式为 JSON: {ruleId, features}"
    - "响应格式为 JSON: {decision, reason, executionTimeMs}"
    - "API 响应时间 < 50ms（P95）"
    - "所有单元测试和集成测试通过"
  artifacts:
    - path: "src/main/java/com/example/ruleengine/controller/DecisionController.java"
      provides: "决策 API 控制器"
      contains: "@RestController"
      contains: "@PostMapping(\"/api/v1/decide\")"
    - path: "src/main/resources/application.yml"
      provides: "应用配置"
      contains: "server.port: 8080"
      contains: "management.endpoints.web.exposure.include"
  key_links:
    - from: "DecisionController.java"
      to: "RuleExecutionService"
      via: "依赖注入"
      pattern: "private.*RuleExecutionService"
    - from: "DecisionController.java"
      to: "DecisionRequest"
      via: "请求体绑定"
      pattern: "@RequestBody.*DecisionRequest"
    - from: "DecisionController.java"
      to: "DecisionResponse"
      via: "响应体"
      pattern: "DecisionResponse|ResponseEntity"
---

## 目标

实现同步决策 API，提供 REST 接口供外部系统调用，确保在 50ms 内返回决策结果。

## 目的

建立规则引擎的对外接口，通过 HTTP POST 请求接收决策请求，返回规则执行结果，满足实时决策场景的性能要求。

## 输出

- POST /api/v1/decide REST API
- 请求/响应 JSON 格式定义
- API 文档（通过 Swagger/OpenAPI 或注释）
- 完整的单元测试和集成测试覆盖

## 执行上下文

@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

## 上下文

@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-engine/01-CONTEXT.md
@.planning/phases/01-core-engine/01-RESEARCH.md

## API 定义

### POST /api/v1/decide

**请求格式（D-17）:**
```json
{
  "ruleId": "rule-001",
  "script": "def result = amount > 1000; return ['decision': result ? 'REJECT' : 'PASS']",
  "features": {
    "amount": 500,
    "user_age": 25,
    "user_level": "VIP"
  },
  "requiredFeatures": ["amount", "user_age", "user_level"],
  "timeoutMs": 50
}
```

**响应格式（D-18）:**
```json
{
  "decision": "PASS",
  "reason": "规则执行完成",
  "executionTimeMs": 15,
  "timeout": false,
  "executionContext": {
    "amount": 500,
    "user_age": 25,
    "user_level": "VIP"
  }
}
```

**性能要求（D-19）:**
- P95 延迟 < 50ms
- P99 延迟 < 100ms
- 超时时间默认 50ms（可配置）

## 任务

<task type="auto">
  <name>Task 1: 实现 DecisionController 决策 API 控制器</name>
  <files>
    src/main/java/com/example/ruleengine/controller/DecisionController.java
  </files>
  <read_first>
    .planning/phases/01-core-engine/01-CONTEXT.md（提取 API 设计决策）
    src/main/java/com/example/ruleengine/service/RuleExecutionService.java（理解服务接口）
    src/main/java/com/example/ruleengine/model/DecisionRequest.java（理解请求模型）
    src/main/java/com/example/ruleengine/model/DecisionResponse.java（理解响应模型）
  </read_first>
  <action>
    创建决策 API 控制器，基于 CONTEXT.md API 设计：

    **src/main/java/com/example/ruleengine/controller/DecisionController.java:**
    ```java
    package com.example.ruleengine.controller;

    import com.example.ruleengine.model.DecisionRequest;
    import com.example.ruleengine.model.DecisionResponse;
    import com.example.ruleengine.service.RuleExecutionService;
    import io.micrometer.core.annotation.Timed;
    import jakarta.validation.Valid;
    import org.slf4j.Logger;
    import org.slf4j.LoggerFactory;
    import org.springframework.http.MediaType;
    import org.springframework.http.ResponseEntity;
    import org.springframework.web.bind.annotation.*;

    /**
     * 决策 API 控制器
     * Source: CONTEXT.md 决策 D-16, D-17, D-18, D-19
     *
     * 功能：
     * 1. REXEC-01: 同步 API 接收决策请求，50ms 内返回结果
     * 2. POST /api/v1/decide 端点
     * 3. JSON 请求/响应格式
     */
    @RestController
    @RequestMapping("/api/v1")
    @Timed(value = "decision.api", description = "Decision API performance")
    public class DecisionController {

        private static final Logger logger = LoggerFactory.getLogger(DecisionController.class);

        private final RuleExecutionService ruleExecutionService;

        public DecisionController(RuleExecutionService ruleExecutionService) {
            this.ruleExecutionService = ruleExecutionService;
        }

        /**
         * 决策 API
         * D-16: POST /api/v1/decide
         * D-17: 请求格式 {ruleId, features}
         * D-18: 响应格式 {decision, reason, executionTimeMs}
         * D-19: API 响应时间要求 < 50ms（P95）
         */
        @PostMapping(
            value = "/decide",
            consumes = MediaType.APPLICATION_JSON_VALUE,
            produces = MediaType.APPLICATION_JSON_VALUE
        )
        public ResponseEntity<DecisionResponse> decide(@Valid @RequestBody DecisionRequest request) {
            logger.info("Received decision request for rule: {}", request.getRuleId());

            try {
                DecisionResponse response = ruleExecutionService.executeDecision(request);

                logger.debug("Rule {} executed in {}ms, decision: {}",
                    request.getRuleId(), response.getExecutionTimeMs(), response.getDecision());

                return ResponseEntity.ok(response);

            } catch (Exception e) {
                logger.error("Decision request failed for rule: {}", request.getRuleId(), e);

                // 返回错误响应
                DecisionResponse errorResponse = DecisionResponse.builder()
                    .decision("REJECT")
                    .reason("决策请求失败: " + e.getMessage())
                    .executionTimeMs(0)
                    .build();

                return ResponseEntity.internalServerError().body(errorResponse);
            }
        }

        /**
         * 健康检查端点
         */
        @GetMapping("/health")
        public ResponseEntity<String> health() {
            return ResponseEntity.ok("OK");
        }
    }
    ```

    关键实现要点：
    1. POST /api/v1/decide 端点（D-16）
    2. JSON 请求/响应格式（D-17, D-18）
    3. 使用 @Timed 注解监控 API 性能
    4. 异常处理和错误响应
  </action>
  <verify>
    <automated>./gradlew test --tests "*DecisionControllerTest"</automated>
  </verify>
  <done>
    - DecisionController 类已创建
    - POST /api/v1/decide 端点已实现
    - 请求/响应格式符合定义
    - 单元测试覆盖正常流程和异常场景
  </done>
  <acceptance_criteria>
    - src/main/java/com/example/ruleengine/controller/DecisionController.java exists
    - DecisionController.java contains "@RestController"
    - DecisionController.java contains "@PostMapping(\"/api/v1/decide\")"
    - DecisionController.java contains "@RequestBody DecisionRequest"
    - DecisionController.java contains "ResponseEntity<DecisionResponse>"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 配置 Spring Boot Actuator 和监控</name>
  <files>
    src/main/resources/application.yml
  </files>
  <read_first>
    src/main/resources/application.yml（查看现有配置）
  </read_first>
  <action>
    更新 application.yml，完善 Actuator 和监控配置：

    在现有 application.yml 基础上，添加或更新以下配置：

    ```yaml
    # Source: RESEARCH.md 标准技术栈 + CONTEXT.md Claude's Discretion
    spring:
      application:
        name: rule-engine

      threads:
        virtual:
          enabled: true

      cache:
        type: caffeine
        caffeine:
          spec: maximumSize=1000,expireAfterWrite=24h

    management:
      endpoints:
        web:
          exposure:
            include: health,info,metrics,prometheus
          base-path: /actuator
      metrics:
        export:
          prometheus:
            enabled: true
        tags:
          application: ${spring.application.name}
        distribution:
          percentiles-histogram:
            http.server.requests: true
          percentiles:
            http.server.requests: 0.5, 0.95, 0.99  # P50, P95, P99
          sla:
            http.server.requests: 50ms, 100ms, 200ms  # 性能阈值

    server:
      port: 8080
      tomcat:
        threads:
          max: 200
          min-spare: 10
        accept-count: 100
        connection-timeout: 10s  # 10 秒连接超时

    logging:
      level:
        root: INFO
        com.example.ruleengine: DEBUG
        org.springframework.web: INFO
      pattern:
        console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
    ```

    关键配置要点：
    1. Actuator 端点：health, info, metrics, prometheus
    2. Prometheus 指标导出
    3. HTTP 请求性能监控（P50, P95, P99）
    4. Tomcat 线程池配置（虚拟线程下可以设置更高的 max）
  </action>
  <verify>
    <automated>./gradlew bootRun --args='--spring.profiles.active=test' &amp;&amp; sleep 10 &amp;&amp; curl -s http://localhost:8080/actuator/health | grep -q "UP" &amp;&amp; pkill -f rule-engine</automated>
  </verify>
  <done>
    - Actuator 端点已配置
    - Prometheus 指标已启用
    - HTTP 请求性能监控已配置
    - 应用可成功启动
  </done>
  <acceptance_criteria>
    - application.yml contains "management.endpoints.web.exposure.include: health,info,metrics,prometheus"
    - application.yml contains "management.metrics.export.prometheus.enabled: true"
    - application.yml contains "percentiles: 0.5, 0.95, 0.99"
    - application.yml contains "server.port: 8080"
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 实现单元测试和集成测试</name>
  <files>
    src/test/java/com/example/ruleengine/controller/DecisionControllerTest.java
    src/test/java/com/example/ruleengine/integration/DecisionControllerIntegrationTest.java
  </files>
  <read_first>
    src/main/java/com/example/ruleengine/controller/DecisionController.java（理解 API 接口）
    src/test/resources/application-test.yml（理解测试配置）
  </read_first>
  <action>
    创建单元测试和集成测试：

    **src/test/java/com/example/ruleengine/controller/DecisionControllerTest.java:**
    ```java
    package com.example.ruleengine.controller;

    import com.example.ruleengine.model.DecisionRequest;
    import com.example.ruleengine.model.DecisionResponse;
    import com.example.ruleengine.service.RuleExecutionService;
    import org.junit.jupiter.api.Test;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
    import org.springframework.boot.test.mock.mockito.MockBean;
    import org.springframework.http.MediaType;
    import org.springframework.test.web.servlet.MockMvc;

    import static org.mockito.ArgumentMatchers.any;
    import static org.mockito.Mockito.when;
    import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.post;
    import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

    /**
     * DecisionController 单元测试
     */
    @WebMvcTest(DecisionController.class)
    class DecisionControllerTest {

        @Autowired
        private MockMvc mockMvc;

        @MockBean
        private RuleExecutionService ruleExecutionService;

        @Test
        void testDecideEndpointReturnsPass() throws Exception {
            // Mock 服务返回
            DecisionResponse response = DecisionResponse.builder()
                .decision("PASS")
                .reason("规则执行完成")
                .executionTimeMs(15)
                .build();

            when(ruleExecutionService.executeDecision(any(DecisionRequest.class)))
                .thenReturn(response);

            // 执行请求
            mockMvc.perform(post("/api/v1/decide")
                    .contentType(MediaType.APPLICATION_JSON)
                    .content("{\"ruleId\":\"rule-001\",\"script\":\"return true\",\"features\":{},\"requiredFeatures\":[]}"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.decision").value("PASS"))
                .andExpect(jsonPath("$.reason").value("规则执行完成"))
                .andExpect(jsonPath("$.executionTimeMs").value(15));
        }

        @Test
        void testDecideEndpointReturnsReject() throws Exception {
            DecisionResponse response = DecisionResponse.builder()
                .decision("REJECT")
                .reason("金额超过阈值")
                .executionTimeMs(10)
                .build();

            when(ruleExecutionService.executeDecision(any(DecisionRequest.class)))
                .thenReturn(response);

            mockMvc.perform(post("/api/v1/decide")
                    .contentType(MediaType.APPLICATION_JSON)
                    .content("{\"ruleId\":\"rule-002\",\"script\":\"return false\",\"features\":{},\"requiredFeatures\":[]}"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.decision").value("REJECT"));
        }

        @Test
        void testHealthEndpoint() throws Exception {
            mockMvc.perform(get("/api/v1/health"))
                .andExpect(status().isOk())
                .andExpect(content().string("OK"));
        }
    }
    ```

    **src/test/java/com/example/ruleengine/integration/DecisionControllerIntegrationTest.java:**
    ```java
    package com.example.ruleengine.integration;

    import com.example.ruleengine.model.DecisionRequest;
    import com.example.ruleengine.model.DecisionResponse;
    import com.example.ruleengine.service.RuleExecutionService;
    import org.junit.jupiter.api.Test;
    import org.springframework.beans.factory.annotation.Autowired;
    import org.springframework.boot.test.context.SpringBootTest;
    import org.springframework.boot.test.web.client.TestRestTemplate;
    import org.springframework.boot.test.web.server.LocalServerPort;
    import org.springframework.http.ResponseEntity;

    import static org.junit.jupiter.api.Assertions.*;

    /**
     * DecisionController 集成测试
     * Source: RESEARCH.md 测试框架 + REXEC-01 验证
     */
    @SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
    class DecisionControllerIntegrationTest {

        @LocalServerPort
        private int port;

        @Autowired
        private TestRestTemplate restTemplate;

        @Autowired
        private RuleExecutionService ruleExecutionService;

        @Test
        void testDecideApiEndToEnd() {
            // 准备请求
            DecisionRequest request = new DecisionRequest();
            request.setRuleId("integration-test-rule");
            request.setScript("def result = amount > 1000; return ['decision': result ? 'REJECT' : 'PASS', 'reason': '金额检查']");
            request.setFeatures(Map.of("amount", 500));
            request.setRequiredFeatures(List.of("amount"));

            // 发送请求
            String url = "http://localhost:" + port + "/api/v1/decide";
            ResponseEntity<DecisionResponse> response = restTemplate.postForEntity(url, request, DecisionResponse.class);

            // 验证响应
            assertNotNull(response.getBody());
            assertEquals("PASS", response.getBody().getDecision());
            assertEquals("金额检查", response.getBody().getReason());
            assertTrue(response.getBody().getExecutionTimeMs() < 50, "API 应该在 50ms 内返回");

            System.out.println("Execution time: " + response.getBody().getExecutionTimeMs() + "ms");
        }

        @Test
        void testDecideApiPerformance() {
            // 性能测试：验证 P95 延迟 < 50ms
            DecisionRequest request = new DecisionRequest();
            request.setRuleId("perf-test-rule");
            request.setScript("return ['decision': 'PASS', 'reason': 'OK']");
            request.setFeatures(Map.of());
            request.setRequiredFeatures(List.of());

            String url = "http://localhost:" + port + "/api/v1/decide";

            // 执行 100 次请求
            int iterations = 100;
            long[] durations = new long[iterations];

            for (int i = 0; i < iterations; i++) {
                long startTime = System.nanoTime();
                ResponseEntity<DecisionResponse> response = restTemplate.postForEntity(url, request, DecisionResponse.class);
                long duration = (System.nanoTime() - startTime) / 1_000_000;  // 转换为毫秒
                durations[i] = duration;

                assertEquals("PASS", response.getBody().getDecision());
            }

            // 计算 P95
            Arrays.sort(durations);
            long p95 = durations[(int) (iterations * 0.95)];

            System.out.println("P50: " + durations[(int) (iterations * 0.50)] + "ms");
            System.out.println("P95: " + p95 + "ms");
            System.out.println("P99: " + durations[(int) (iterations * 0.99)] + "ms");

            assertTrue(p95 < 50, "P95 延迟应该 < 50ms，实际: " + p95 + "ms");
        }
    }
    ```

    关键测试要点：
    1. 单元测试：Mock 服务层，测试控制器逻辑
    2. 集成测试：端到端测试，验证 API 性能
    3. 性能测试：验证 P95 延迟 < 50ms（REXEC-01）
  </action>
  <verify>
    <automated>./gradlew test --tests "*DecisionControllerTest" --tests "*DecisionControllerIntegrationTest"</automated>
  </verify>
  <done>
    - 单元测试已创建，覆盖正常流程和异常场景
    - 集成测试已创建，验证端到端流程
    - 性能测试已创建，验证 50ms 目标
    - 所有测试通过
  </done>
  <acceptance_criteria>
    - src/test/java/com/example/ruleengine/controller/DecisionControllerTest.java exists
    - DecisionControllerTest.java contains "@WebMvcTest"
    - DecisionControllerTest.java contains "mockMvc.perform"
    - src/test/java/com/example/ruleengine/integration/DecisionControllerIntegrationTest.java exists
    - DecisionControllerIntegrationTest.java contains "@SpringBootTest"
    - DecisionControllerIntegrationTest.java contains "TestRestTemplate"
    - Command "./gradlew test --tests '*DecisionController*'" exits with code 0
  </acceptance_criteria>
</task>

## 验证

完成上述任务后，运行以下验证步骤：

1. **启动应用:**
   ```bash
   ./gradlew bootRun
   ```
   确保应用成功启动。

2. **测试决策 API:**
   ```bash
   curl -X POST http://localhost:8080/api/v1/decide \
     -H "Content-Type: application/json" \
     -d '{
       "ruleId": "test-rule",
       "script": "def result = amount > 1000; return [\"decision\": result ? \"REJECT\" : \"PASS\", \"reason\": \"金额检查\"]",
       "features": {"amount": 500},
       "requiredFeatures": ["amount"]
     }'
   ```
   预期返回：
   ```json
   {
     "decision": "PASS",
     "reason": "金额检查",
     "executionTimeMs": 15,
     "timeout": false,
     "executionContext": {"amount": 500}
   }
   ```

3. **验证 Actuator 端点:**
   ```bash
   # 健康检查
   curl http://localhost:8080/actuator/health

   # Prometheus 指标
   curl http://localhost:8080/actuator/metrics/http.server.requests

   # 应用信息
   curl http://localhost:8080/actuator/info
   ```

4. **性能验证:**
   使用 Apache Bench 或 wrk 进行压力测试：
   ```bash
   # 安装 Apache Bench
   brew install httpd

   # 压力测试（1000 个请求，100 并发）
   ab -n 1000 -c 100 -p request.json -T application/json \
     http://localhost:8080/api/v1/decide
   ```
   其中 request.json 内容：
   ```json
   {
     "ruleId": "perf-rule",
     "script": "return [\"decision\": \"PASS\", \"reason\": \"OK\"]",
     "features": {},
     "requiredFeatures": []
   }
   ```
   验证 P95 延迟 < 50ms。

## 成功标准

1. POST /api/v1/decide API 可访问且正常工作
2. API 响应时间 P95 < 50ms
3. Actuator 端点可访问
4. Prometheus 指标正常导出
5. 所有单元测试和集成测试通过
6. 压力测试通过，满足性能目标

## 输出

完成后创建 `.planning/phases/01-core-engine/01-05-SUMMARY.md`
