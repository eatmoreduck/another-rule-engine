---
phase: 03-rule-config-ui
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - frontend/src/utils/dslGenerator.ts
  - frontend/src/utils/flowDslGenerator.ts
  - frontend/src/components/rules/ScriptPreview.tsx
  - frontend/src/components/rules/ModeSwitch.tsx
  - frontend/src/pages/RuleEditPage.tsx
  - frontend/src/pages/FlowEditorPage.tsx
  - frontend/src/pages/RuleListPage.tsx
  - frontend/src/pages/RuleDetailPage.tsx
  - frontend/src/App.tsx
  - frontend/src/styles/editor.css
autonomous: false
requirements:
  - RCONF-01
  - RCONF-02
  - UI-03

must_haves:
  truths:
    - "用户可以在表单模式和流程图模式之间切换"
    - "两种模式生成的 Groovy DSL 格式一致"
    - "规则列表页可以进入表单编辑或流程图编辑"
    - "规则详情页显示正确的 Groovy 脚本并提供编辑入口"
    - "前端应用可以构建并正常运行"
  artifacts:
    - path: "frontend/src/components/rules/ModeSwitch.tsx"
      provides: "编辑模式切换组件"
      contains: "form.*flow|模式切换"
    - path: "frontend/src/pages/RuleEditPage.tsx"
      provides: "完整规则编辑页面（含模式切换）"
      contains: "ModeSwitch"
    - path: "frontend/src/styles/editor.css"
      provides: "编辑器样式（脚本预览高亮等）"
      contains: "script-preview"
  key_links:
    - from: "frontend/src/pages/RuleListPage.tsx"
      to: "frontend/src/pages/RuleEditPage.tsx"
      via: "导航路由 /rules/new 和 /rules/new/flow"
      pattern: "navigate.*rules"
    - from: "frontend/src/pages/RuleDetailPage.tsx"
      to: "frontend/src/pages/RuleEditPage.tsx"
      via: "编辑按钮跳转"
      pattern: "navigate.*edit"
---

<objective>
集成表单编辑器和流程图编辑器，实现双模式无缝切换，完善规则管理流程和脚本预览体验。

Purpose: 实现 D-06 双模式配置的核心集成。确保两种编辑模式一致、切换流畅，规则管理页面（列表、详情、编辑）形成完整闭环。
Output: 模式切换组件、完善的路由导航、统一样式、生产可构建的前端应用。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rule-config-ui/03-CONTEXT.md
@.planning/phases/03-rule-config-ui/03-01-SUMMARY.md
@.planning/phases/03-rule-config-ui/03-02-SUMMARY.md
@.planning/phases/03-rule-config-ui/03-03-SUMMARY.md

<interfaces>
<!-- 从 03-02 计划产出的接口 -->
From frontend/src/utils/dslGenerator.ts:
```typescript
export function generateGroovyFromForm(config: FormRuleConfig): string
```

From frontend/src/types/ruleConfig.ts:
```typescript
export interface FormRuleConfig {
  defaultAction: Action;
  rules: ConditionActionRule[];
}
```

<!-- 从 03-03 计划产出的接口 -->
From frontend/src/utils/flowDslGenerator.ts:
```typescript
export function generateGroovyFromFlow(nodes: FlowNode[], edges: Edge[]): string
```

From frontend/src/types/flowConfig.ts:
```typescript
export type FlowNode = Node<ConditionNodeData | ActionNodeData | StartNodeData | EndNodeData>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 统一 DSL 生成和完善脚本预览组件</name>
  <read_first>
    @frontend/src/utils/dslGenerator.ts
    @frontend/src/utils/flowDslGenerator.ts
    @frontend/src/components/rules/ScriptPreview.tsx
  </read_first>
  <files>
    frontend/src/utils/dslGenerator.ts
    frontend/src/utils/flowDslGenerator.ts
    frontend/src/components/rules/ScriptPreview.tsx
    frontend/src/styles/editor.css
  </files>
  <action>
    Per D-09: 两种模式最终都生成 Groovy DSL 脚本，统一通过后端引擎执行。
    Per D-16: 支持预览生成的 Groovy 脚本。
    Per D-17: 生成的脚本需通过后端沙箱安全检查。

    1. 确保两个 DSL 生成器输出格式一致：
       - dslGenerator.ts 和 flowDslGenerator.ts 都生成以下格式：
         ```groovy
         def evaluate(Map features) {
           // 规则逻辑
           return [decision: 'PASS|REJECT|MANUAL_REVIEW', reason: '...']
         }
         ```
       - 提取共享的运算符映射和类型转换逻辑到 dslGenerator.ts 中导出，flowDslGenerator.ts 引用
       - 添加公共函数 `generateScriptHeader(): string` 和 `generateReturnStatement(decision, reason): string`

    2. 改进 ScriptPreview.tsx 组件：
       - 使用 Ant Design Drawer 代替 Modal（侧边弹出更符合编辑器体验）
       - 脚本区域使用深色背景 + 等宽字体 + 语法高亮颜色：
         - 关键字(def, if, else, return): 蓝色
         - 字符串: 绿色
         - 数字: 橙色
         - 注释: 灰色
       - 使用正则表达式实现简单语法高亮（不引入外部库）
       - 添加 "验证脚本" 按钮：调用 validateScript API，显示验证结果
       - 添加 "复制" 按钮：复制脚本到剪贴板
       - Drawer 标题显示 "Groovy 脚本预览"
       - Drawer 宽度 600px

    3. 创建 src/styles/editor.css：
       - .script-preview-code: 深色背景(#1e1e1e), 浅色文字(#d4d4d4), 等宽字体, padding, border-radius
       - .script-keyword: color #569cd6 (蓝色关键字)
       - .script-string: color #ce9178 (橙色字符串)
       - .script-number: color #b5cea8 (绿色数字)
       - .script-comment: color #6a9955 (灰色注释)
       - .mode-switch: 编辑模式切换区域样式
       - .node-palette: 节点面板样式
       - 确保在 main.tsx 中导入此 CSS 文件
  </action>
  <verify>
    <automated>cd /Users/leon/Documents/Justforfun/another-rule-engine/frontend && npx tsc --noEmit && grep -c "generateScriptHeader\|generateReturnStatement" src/utils/dslGenerator.ts src/utils/flowDslGenerator.ts</automated>
  </verify>
  <done>
    - 两个 DSL 生成器使用共享的辅助函数，输出格式一致
    - ScriptPreview 使用 Drawer 展示，带语法高亮
    - 脚本验证功能正常
    - editor.css 提供编辑器相关样式
    - TypeScript 编译通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 实现双模式切换和完整路由集成</name>
  <read_first>
    @frontend/src/App.tsx
    @frontend/src/pages/RuleListPage.tsx
    @frontend/src/pages/RuleDetailPage.tsx
    @frontend/src/pages/RuleEditPage.tsx
    @frontend/src/pages/FlowEditorPage.tsx
  </read_first>
  <files>
    frontend/src/components/rules/ModeSwitch.tsx
    frontend/src/pages/RuleListPage.tsx
    frontend/src/pages/RuleDetailPage.tsx
    frontend/src/pages/RuleEditPage.tsx
    frontend/src/pages/FlowEditorPage.tsx
    frontend/src/App.tsx
  </files>
  <action>
    Per D-06: 双模式配置，简单规则用表单，复杂决策流用流程图。
    Per D-10: 业务人员不直接接触 Groovy 代码。
    Per D-12: 主要页面完整。
    Per D-13: 顶部导航包含所有页面。

    1. 创建 src/components/rules/ModeSwitch.tsx：
       - Ant Design Segmented 或 Radio.Group 组件
       - 选项："表单模式" / "流程图模式"
       - 切换时通过路由跳转：
         - 表单 -> /rules/:ruleKey/edit 或 /rules/new
         - 流程图 -> /rules/:ruleKey/edit/flow 或 /rules/new/flow
       - 提示：切换模式时如果当前有未保存的修改，使用 Modal.confirm 提示用户

    2. 更新 RuleListPage.tsx：
       - "新建规则" 按钮改为 DropdownButton（或 Button.Group）：
         - "表单创建" -> /rules/new
         - "可视化创建" -> /rules/new/flow
       - 表格操作列增加两个编辑入口：
         - "编辑" -> /rules/:ruleKey/edit
         - "可视化编辑" -> /rules/:ruleKey/edit/flow
       - 增加面包屑导航

    3. 更新 RuleDetailPage.tsx：
       - 操作按钮区域：
         - "表单编辑" -> /rules/:ruleKey/edit
         - "可视化编辑" -> /rules/:ruleKey/edit/flow
         - "启用/禁用" 切换按钮
         - "删除" 按钮（带确认）
       - 增加版本历史 Tab（如果有版本管理 API）
       - Groovy 脚本区域添加 "查看脚本" 折叠面板

    4. 更新 RuleEditPage.tsx：
       - 页面顶部右侧添加 ModeSwitch 组件
       - 编辑器区域下方显示 "脚本预览" 折叠面板
       - 保存成功后跳转到规则详情页
       - 添加页面离开确认（未保存时提示）

    5. 更新 FlowEditorPage.tsx：
       - 页面顶部右侧添加 ModeSwitch 组件
       - 画布下方显示实时生成的脚本预览
       - 保存成功后跳转到规则详情页
       - 添加页面离开确认

    6. 完善路由配置 App.tsx：
       ```typescript
       <BrowserRouter>
         <Routes>
           <Route element={<MainLayout />}>
             <Route index element={<Navigate to="/rules" replace />} />
             <Route path="rules" element={<RuleListPage />} />
             <Route path="rules/new" element={<RuleEditPage />} />
             <Route path="rules/new/flow" element={<FlowEditorPage />} />
             <Route path="rules/:ruleKey" element={<RuleDetailPage />} />
             <Route path="rules/:ruleKey/edit" element={<RuleEditPage />} />
             <Route path="rules/:ruleKey/edit/flow" element={<FlowEditorPage />} />
           </Route>
         </Routes>
       </BrowserRouter>
       ```
       - 添加 404 fallback 路由
  </action>
  <verify>
    <automated>cd /Users/leon/Documents/Justforfun/another-rule-engine/frontend && npx tsc --noEmit && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - ModeSwitch 组件支持表单/流程图模式切换
    - 规则列表页提供两种编辑入口
    - 规则详情页提供完整操作按钮
    - 编辑页面支持模式切换和脚本预览
    - 路由配置完整，覆盖所有页面路径
    - 前端生产构建成功
    - TypeScript 编译通过
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npx tsc --noEmit` -- TypeScript 编译无错误
2. `cd frontend && npx vite build` -- 生产构建成功
3. 检查路由配置覆盖所有页面路径
4. 检查模式切换组件正确跳转
5. 检查 DSL 生成器输出格式一致
</verification>

<success_criteria>
- 双模式（表单/流程图）可自由切换
- 两种模式生成的 Groovy DSL 格式一致
- 规则管理流程完整：列表 -> 详情 -> 编辑 -> 保存
- 编辑页面提供脚本预览和验证功能
- 前端应用可成功构建
- 所有页面路由正确配置
- TypeScript 编译通过
</success_criteria>

<output>
After completion, create `.planning/phases/03-rule-config-ui/03-04-SUMMARY.md`
</output>
